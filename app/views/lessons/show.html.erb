<div id="middle" class="container">
  <div class="white">

    <header class="page-header">
      <h1>Lesson Details</h1>
    </header>
    <% if @lesson.waiting_for_payment? && current_user == @lesson.requester %>
      <%= render partial: 'make_payment' %>
      <% elsif current_user.instructor %>
          <%= link_to "Edit Lesson Times", mark_lesson_complete_path, method: :put, class: "btn btn-success" %>
    <% end %>

    <%= render partial: 'lesson_details' %>

    <% if @lesson.active? %>
      <%= render partial: 'lesson_actions_active' %>
    <% end %>

    <% if @lesson.finalizing? %>
      <%= render partial: 'lesson_actions_finalizing' %>
    <% end %>

    <% if @lesson.canceled? %>
      <p>Lesson has been canceled.</p>
    <% end %>

  </div>
</div>

<!-- SLIDER -->
<img id="cycle-loader" src="/assets/spinner.gif" alt="loader" />

<ul id="slider">
  <li><img src="/assets/demo/slider/surfing-bright.jpg" alt="" /></li>
</ul>
<!-- /SLIDER
